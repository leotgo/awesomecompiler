//comando de fluxo de controle if com saída com declaração de variável local
int minha_funcao (float k, int l) 
{
        a=5;
        p=a;
        if (a==0)
        then output x
        else output "nada";
        if (a==0)
        then { float x; output x; };
}
