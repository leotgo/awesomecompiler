char input_string[100];

int countwords(char separator, int length)
{	
	int words;
	words = 0;

	int i;
	i = 0;
	while ( i < length ) do 
	{
		if ( input_string[i] == separator ) then 
		{
			words = words + 1;
		} ;

		i = i + 1;
	} ;

	return words;
}
